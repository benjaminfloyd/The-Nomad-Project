<h1>Edit a Post</h1>


<%= form_for @post, url: {action: "update"} do |f| %>
<%= f.text_field :title%>
<%= f.text_field :post%>
<%= f.text_field :img%>
<%= f.submit%> 

<% end %>

<%= button_to 'Delete Post', [@city, @post], class: 'alert button', method: :delete, data: { confirm: "About to delete #{@post.title}! Are you sure you want to do this?" } %>